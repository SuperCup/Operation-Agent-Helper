# 运营Agent交互流程设计文档

## 一、整体架构设计

### 1.1 页面布局
```
┌─────────────────────────────────────────────────────────┐
│  顶部栏（客户切换、系统状态）                              │
├──────────────┬──────────────────────────────────────────┤
│              │                                          │
│  侧边栏      │  主内容区                                 │
│  - 工作台    │  ┌──────────────────┬──────────────────┐ │
│  - 任务管理  │  │                  │                  │ │
│              │  │  Agent对话区      │  快捷功能栏      │ │
│              │  │  (左侧2/3)        │  (右侧1/3)       │ │
│              │  │                  │  - 生成PPT       │ │
│              │  │                  │  - 生成Excel      │ │
│              │  │                  │  - 生成Doc       │ │
│              │  │                  │                  │ │
│              │  │                  │  ──────────────  │ │
│              │  │                  │  耗时任务列表    │ │
│              │  │                  │  (在快捷功能下方)│ │
│              │  └──────────────────┴──────────────────┘ │
└──────────────┴──────────────────────────────────────────┘
```

### 1.2 核心状态管理

#### 会话状态 (ConversationState)
```typescript
interface ConversationState {
  // 会话基本信息
  sessionId: string | null;           // 当前会话ID
  sessionTitle: string;                // 会话标题（用于归档）
  isNewSession: boolean;               // 是否为新会话
  hasActiveTask: boolean;              // 是否有进行中的任务
  
  // 意图识别状态
  intentStatus: 'idle' | 'recognizing' | 'identified' | 'unclear';
  identifiedIntent?: IntentType;       // 已识别的意图
  recommendedAgents?: AgentConfig[];    // 推荐的Agent列表
  
  // 当前任务状态
  currentTask?: {
    id: string;
    type: TaskType;                    // 'operation_plan' | 'budget_split' | ...
    agentId: string;                    // 当前使用的Agent
    workflowId?: string;                // 工作流ID
    status: 'collecting' | 'running' | 'completed' | 'archived';
    collectedParams?: Record<string, any>; // 已收集的参数
  };
  
  // 信息收集状态
  infoCollection?: {
    agentId: string;
    requiredParams: ParamDefinition[];
    collectedParams: Record<string, any>;
    step: number;                       // 当前收集步骤
  };
}
```

#### 任务状态 (TaskState)
```typescript
interface TaskState {
  // 后台任务（耗时任务）
  backgroundTasks: BackgroundTask[];
  
  // 文档生成任务
  documentGenerationTasks: {
    id: string;
    type: 'ppt' | 'excel' | 'doc';
    sourceTaskId: string;              // 来源任务ID
    status: 'pending' | 'generating' | 'completed' | 'failed';
    progress: number;
    result?: {
      url: string;
      previewUrl?: string;
    };
  }[];
}
```

## 二、详细交互流程

### 2.1 场景一：用户首次进入工作台

#### 流程步骤：

**步骤1：页面初始化**
- 系统检测到用户首次进入（无历史会话记录）
- 显示Agent欢迎语
- 展示快捷功能栏（生成PPT、生成Excel、生成Doc）- 初始状态为禁用

**步骤2：Agent欢迎语**
```
欢迎语内容：
"您好！我是小琳，您的即时零售运营AI Agent助手。

我可以帮您完成以下工作：
• 运营方案生成
• 预算拆分
• 活动配置
• RTB方案
• RTB配置
• 活动运营
• RTB运营

请告诉我您想要做什么？或者您可以直接描述您的需求。"
```

**步骤3：用户输入需求**
- 用户在对话框中输入需求（文字描述）
- 支持上传文件（历史方案、素材图片等）

**步骤4：意图识别**
- Agent进行意图识别（调用意图识别Agent）
- 显示思考过程："正在分析您的需求..."

**分支A：需求模糊**
- 如果意图识别结果置信度 < 0.7，判定为需求模糊
- Agent回复：
  ```
  "我理解您想要完成运营相关工作，但需要更多信息来确定具体任务。
  
  以下是我的能力范围，请选择您想要执行的任务：
  
  [能力卡片展示]
  ┌─────────────┬─────────────┬─────────────┐
  │ 运营方案生成 │ 预算拆分    │ 活动配置    │
  │ [选择按钮]   │ [选择按钮]   │ [选择按钮]   │
  └─────────────┴─────────────┴─────────────┘
  ┌─────────────┬─────────────┬─────────────┐
  │ RTB方案      │ RTB配置     │ 活动运营    │
  │ [选择按钮]   │ [选择按钮]   │ [选择按钮]   │
  └─────────────┴─────────────┴─────────────┘
  ┌─────────────┐
  │ RTB运营      │
  │ [选择按钮]   │
  └─────────────┘
  ```
- 用户点击选择后，进入信息收集流程

**分支B：需求清晰**
- 如果意图识别结果置信度 ≥ 0.7，判定为需求清晰
- Agent复述用户目的：
  ```
  "我理解您想要[复述用户目的]。
  
  我推荐使用以下Agent来完成这个任务：
  
  [推荐Agent卡片]
  ┌─────────────────────────────────────┐
  │ 🎯 运营方案生成Agent                 │
  │ 适用于：项目准备阶段，生成完整方案   │
  │ [使用此Agent] 按钮                   │
  └─────────────────────────────────────┘
  
  或者，您也可以选择其他相关Agent：
  [其他Agent列表]
  ```
- 用户确认后，进入信息收集流程

**步骤5：信息收集**
- 根据选定的Agent和工作流模板，系统确定需要收集的参数（基于工作流入参）
- 弹出信息收集模态框（一次性收集所有必要信息）
- 表单字段根据工作流模板自动生成

**信息收集模态框设计：**
```
┌─────────────────────────────────────────────┐
│  信息收集 - 运营方案生成                    │
├─────────────────────────────────────────────┤
│                                             │
│  📋 基本信息                                │
│  ┌─────────────────────────────────────┐   │
│  │ 项目名称: [___________]             │   │
│  │ 品牌: [下拉选择]                    │   │
│  │ 平台: [美团/饿了么/京东/抖音]        │   │
│  │ 预算范围: [___________] 万元        │   │
│  │ 时间周期: [开始日期] 至 [结束日期]  │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  📎 参考资料                                │
│  ┌─────────────────────────────────────┐   │
│  │ [上传历史参考方案]                   │   │
│  │ [上传方案素材/场景截图]              │   │
│  │ [上传数据来源文件]                   │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  💡 可选信息                                │
│  ┌─────────────────────────────────────┐   │
│  │ 特殊要求: [多行文本输入]             │   │
│  │ 目标指标: [GMV/ROI/转化率等]        │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  [取消]  [确认并开始生成]                   │
└─────────────────────────────────────────────┘
```

**步骤6：用户确认并开始工作流**
- 用户点击"确认并开始生成"
- 关闭模态框
- 在对话框中显示工作流执行过程（支持查看Agent思考过程）：
  ```
  "✅ 已收集所有必要信息，开始生成运营方案..."
  
  [工作流执行过程展示]
  ┌─────────────────────────────────────┐
  │ 🔍 步骤1: 分析项目信息...            │
  │ ✓ 已完成                              │
  ├─────────────────────────────────────┤
  │ 📚 步骤2: 检索历史案例...            │
  │ ⏳ 进行中 (45%)                      │
  ├─────────────────────────────────────┤
  │ 💡 步骤3: 生成运营策略...            │
  │ ⏸ 等待中                             │
  └─────────────────────────────────────┘
  ```

**步骤7：方案框架输出**
- 工作流执行完成后，在对话框中输出方案框架
- 用户可以基于内容进行调整
- 显示调整建议：
  ```
  "📄 运营方案框架已生成：
  
  [方案内容展示，支持编辑]
  
  💡 您可以：
  • 直接编辑方案内容
  • 点击右侧"生成PPT"按钮创建PPT文档
  • 继续对话进行优化"
  ```

**步骤8：生成PPT（快捷功能）**
- 用户点击右侧"生成PPT"按钮
- 创建PPT生成任务，显示在右侧耗时任务列表中
- 对话框提示："已创建PPT生成任务，正在生成中..."
- 任务完成后，用户可点击任务卡片进行预览

### 2.2 场景二：用户二次进入工作台

#### 流程步骤：

**步骤1：检测历史会话**
- 系统检测到存在未归档的会话记录
- 显示会话恢复提示（如果任务已完成，则自动归档并开启新会话）：

```
┌─────────────────────────────────────────────┐
│  💬 检测到您有未完成的会话                   │
├─────────────────────────────────────────────┤
│                                             │
│  会话标题: "春节大促运营方案生成"            │
│  最后活动: 2小时前                          │
│  当前状态: 方案框架已生成，待生成PPT         │
│                                             │
│  [继续此会话]  [开启新会话]                  │
└─────────────────────────────────────────────┘
```

**分支A：继续先前会话**
- 用户点击"继续此会话"
- 恢复对话历史
- 恢复任务状态
- 显示当前任务进度
- 快捷功能栏根据任务状态启用/禁用

**分支B：开启新会话**
- 用户点击"开启新会话"
- 弹出归档确认对话框：
  ```
  ┌─────────────────────────────────────────────┐
  │  归档当前会话                                │
  ├─────────────────────────────────────────────┤
  │                                             │
  │  系统建议标题: "春节大促运营方案生成"        │
  │                                             │
  │  会话标题: [可编辑输入框]                    │
  │                                             │
  │  💡 归档后可在任务管理中查看历史会话         │
  │                                             │
  │  [取消]  [确认归档并开启新会话]              │
  └─────────────────────────────────────────────┘
  ```
- 用户确认后，归档当前会话
- 创建新会话，显示欢迎语

### 2.3 任务归档流程

#### 触发场景：
1. 用户开启新会话时
2. 用户主动点击"归档会话"按钮
3. **任务完成后自动归档**（新增）
4. 工作流中断时自动归档（新增）

#### 归档流程：
**步骤1：系统提示**
- 当用户在执行任务过程中继续对话时，系统检测到任务状态
- 如果当前任务已完成，提示：
  ```
  "✅ 当前任务"运营方案生成"已完成。
  
  您是要：
  • 基于当前任务继续（如：优化方案、生成其他文档）
  • 开启新任务（当前会话将被归档）"
  ```

**步骤2：用户选择开启新任务**
- 弹出归档对话框（同上）
- Agent自动生成归档标题（基于任务类型和内容）
- 用户可修改标题
- 确认归档

**步骤3：归档完成**
- 会话保存到任务管理（包含完整的对话历史、上传文件、生成文件等）
- 创建新会话
- 显示欢迎语

### 2.6 工作流中断处理

#### 触发场景：
- 用户在信息收集阶段取消
- 工作流执行过程中出错
- 用户主动中断

#### 处理流程：
1. 系统检测到工作流中断
2. 自动归档当前会话（静默执行，不弹出确认）
3. 归档标题自动生成（基于当前任务类型和进度）
4. 静默开启新会话
5. 显示欢迎语，引导用户重新描述需求：
   ```
   "您好！我是小琳，您的即时零售运营AI Agent助手。
   
   看起来上一个任务遇到了问题，已为您自动保存。
   
   请告诉我您想要做什么？或者您可以直接描述您的需求。"
   ```

### 2.4 快捷功能交互

#### 生成PPT流程：
1. 用户点击"生成PPT"按钮
2. 检查前置条件：
   - 检查当前任务中是否有支撑生成PPT的信息
   - 如果无，按钮置灰，hover提示："当前任务无可用内容生成PPT"
   - 如果有，启用按钮
3. 创建PPT生成任务
4. 任务显示在右侧耗时任务列表
5. 对话框显示："📊 已创建PPT生成任务"
6. 任务执行过程中显示进度
7. 完成后，任务卡片显示预览按钮
8. 用户点击预览，打开PPT预览模态框

#### 生成Excel/生成Doc流程：
- 与生成PPT流程类似
- 根据内容类型自动选择模板
- 同样根据当前任务内容判断是否启用

### 2.5 耗时任务展示

#### 位置：
- 在右侧快捷功能栏下方
- 固定高度区域，支持滚动

#### 展示内容：
```
┌─────────────────────────────────────┐
│  ⏱️ 耗时任务                         │
├─────────────────────────────────────┤
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 📊 PPT生成任务                │ │
│  │ 进度: 65%                     │ │
│  │ [进度条]                      │ │
│  │ [暂停] [查看详情]             │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 📈 Excel生成任务              │ │
│  │ 状态: 已完成 ✓                │ │
│  │ [预览] [下载] [删除]          │ │
│  └───────────────────────────────┘ │
│                                     │
└─────────────────────────────────────┘
```

## 三、优化建议

### 3.1 交互体验优化

#### 建议1：意图识别优化
**问题**：意图识别可能不够准确，导致频繁弹出能力选择
**优化**：
- 使用独立的意图识别Agent，提高识别准确率
- 增加多轮对话澄清机制
- 支持用户直接说"我要生成运营方案"，无需弹出选择
- 意图识别结果展示置信度，让用户确认

#### 建议2：信息收集优化
**问题**：一次性收集所有信息可能让用户感到压力
**优化方案**：智能预填充
- 基于历史数据自动填充部分信息
- 用户只需确认或修改
- 表单字段分组展示，逻辑清晰

#### 建议3：工作流执行过程展示
**问题**：工作流执行过程需要更清晰的展示
**优化**：
- 实时显示每个步骤的详细日志
- 支持展开/折叠查看详细过程
- **关键节点显示Agent思考过程（支持用户查看）**
- 思考过程可以折叠，默认不展开，避免信息过载

#### 建议4：快捷功能状态管理
**问题**：快捷功能何时启用/禁用需要明确
**优化**：
- 明确的状态提示：
  - 禁用状态：按钮置灰，hover显示原因："当前任务无可用内容生成PPT"
  - 启用状态：正常显示，hover显示可用操作
- 实时检测：每次任务状态更新时，重新检测是否可生成对应格式
- 支持批量生成：如果内容充足，可同时生成PPT、Excel、Doc

### 3.2 功能增强建议

#### 建议5：会话历史管理
**增强**：
- 在侧边栏增加"会话历史"入口
- 支持搜索、筛选、标签管理
- 支持会话导出

#### 建议6：任务模板
**增强**：
- 支持保存常用任务配置为模板
- 下次可直接使用模板快速创建任务

#### 建议7：协作功能
**增强**：
- 支持会话分享
- 支持多人协作编辑方案
- 支持评论和批注

### 3.3 技术实现建议

#### 建议8：状态持久化
- 会话状态需要持久化到本地存储或后端
- 支持断点续传（刷新页面不丢失状态）
- 会话数据包含：文本信息、上传文件、生成文件等完整信息

#### 建议9：错误处理
- 网络错误：自动重试机制
- Agent调用失败：降级方案（使用备用Agent）
- 任务失败：清晰的错误提示和重试选项

#### 建议10：性能优化
- 大量消息时的虚拟滚动
- 图片/文件上传的进度显示
- 长文本内容的懒加载

## 四、问题确认结果

### 4.1 业务逻辑确认

1. **意图识别Agent**：✅ **需要独立的意图识别Agent**
2. **信息收集时机**：✅ **基于子业务场景中的工作流入参，一次生成表单，供用户填写**
3. **任务归档时机**：✅ **除了用户主动归档，也需要在任务完成后自动归档**
4. **快捷功能依赖**：✅ **生成PPT/Excel/Doc不依赖特定任务类型，如果当前任务中没有支撑生成对应输出文件格式的信息，则将对应快捷功能置灰**
5. **工作流中断**：✅ **自动归档，静默开启新会话，并重新引导用户描述需求**

### 4.2 UI/UX确认

1. **信息收集模态框**：✅ **一次性完成**
2. **能力介绍展示**：✅ **使用卡片式**
3. **工作流执行展示**：✅ **需要支持用户查看详细的Agent思考过程**
4. **快捷功能位置**：✅ **固定在右侧**

### 4.3 数据模型确认

1. **会话数据结构**：✅ **所有会话产生的文本信息以及用户上传的文件图片以及Agent生成的文件等**
2. **任务关联**：✅ **一个会话可以包含多个任务**
3. **归档数据**：✅ **归档后的会话需要保留完整的对话历史**

## 五、实施计划

### 阶段一：核心流程实现（优先级：高）
1. 会话状态管理
2. 意图识别流程
3. 信息收集模态框
4. 工作流执行展示
5. 快捷功能基础实现

### 阶段二：优化功能（优先级：中）
1. 会话恢复功能
2. 任务归档功能
3. 耗时任务展示优化
4. 错误处理机制

### 阶段三：增强功能（优先级：低）
1. 会话历史管理
2. 任务模板
3. 协作功能

---

**文档版本**：v1.0  
**创建日期**：2026-01-28  
**最后更新**：2026-01-28
